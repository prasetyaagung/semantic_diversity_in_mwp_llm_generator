# LoRA Fine-tuning + Evaluation

Project ini melakukan fine-tuning model LLM menggunakan LoRA dan mengevaluasi diversity output menggunakan:

- Self-BLEU
- Jaccard Similarity
- Cosine Similarity

## Setup

```bash
pip install -r requirements.txt